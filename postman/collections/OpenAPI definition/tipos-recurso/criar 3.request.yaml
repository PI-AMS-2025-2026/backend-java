$kind: http-request
description: ""
url: "{{baseUrl}}/tipos-recurso"
method: POST
headers:
  Content-Type: application/json
  Accept: "*/*"
body:
  type: json
  content: |-
    {
      "nome": "string"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "nome": {
          "type": "string",
          "minLength": 2,
          "maxLength": 100
        }
      },
      "required": [
        "nome"
      ]
    }
settings: {}
examples: ./.resources/criar 3.resources/examples
order: 3000
