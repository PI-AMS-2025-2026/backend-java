$kind: http-request
description: ""
url: "{{baseUrl}}/tipos-sala"
method: POST
headers:
  Content-Type: application/json
  Accept: "*/*"
body:
  type: json
  content: |-
    {
      "nome": "Sala dos Professores"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "nome": {
          "type": "string",
          "minLength": 2,
          "maxLength": 100
        }
      },
      "required": [
        "nome"
      ]
    }
settings: {}
examples: ./.resources/criar 2.resources/examples
order: 3000
