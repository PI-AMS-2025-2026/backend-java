spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: ''
  jpa:
    defer-datasource-initialization: true
  sql:
    init:
      encoding: UTF-8
  h2:
    console:
      enabled: true
      path: /h2-console
  flyway:
    enabled: false
api:
  security:
    token:
      secret: ${JWT_SECRET:gini}